# auto-generated by grc.converter

id: phychic_message_map
label: Message Map
category: '[phychic]'

parameters:
-   id: function
    label: Function
    dtype: raw

inputs:
-   domain: message
    id: in
    optional: true

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import phychic
    make: phychic.MessageMap(${function})

file_format: 1
