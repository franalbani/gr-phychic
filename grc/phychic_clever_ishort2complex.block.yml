# auto-generated by grc.converter

id: phychic_clever_ishort2complex
label: Clever IShort to Complex
category: '[phychic]'

parameters:
-   id: scale_factor
    label: Scale factor
    dtype: float
    default: 1.0 / C2IS_SCALE_FACTOR

inputs:
-   domain: stream
    dtype: short

outputs:
-   label: signal
    domain: stream
    dtype: complex

templates:
    imports: |-
        from phychic import clever_ishort2complex
        from phychic.clever_complex2ishort import C2IS_SCALE_FACTOR
    make: clever_ishort2complex(${scale_factor})

file_format: 1
