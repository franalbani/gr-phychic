# auto-generated by grc.converter

id: phychic_snapshotlizer
label: Snapshotlizer
category: '[phychic]'

parameters:
-   id: block_size
    label: Block size
    dtype: int
    default: '2048'
-   id: one_in
    label: One in
    dtype: int
    default: '48'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import phychic
    make: phychic.Snapshotlizer(${block_size}, ${one_in})

file_format: 1
