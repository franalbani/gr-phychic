# auto-generated by grc.converter

id: phychic_clipper
label: Clipper
category: '[phychic]'

parameters:
-   id: lower
    label: Lower limit
    dtype: float
    default: '-1.0'
-   id: upper
    label: Upper limit
    dtype: float
    default: '1.0'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   label: signal
    domain: stream
    dtype: complex

templates:
    imports: import phychic
    make: phychic.Clipper(lower=${lower}, upper=${upper})

file_format: 1
